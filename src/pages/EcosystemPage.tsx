import { PageIntro } from '../components/common/PageIntro';import { PortfolioCard } from '../components/ecosystem/PortfolioCard';import { getEntriesByGroup,portfolioGroups } from '../data/portfolioRegistry';import { useDocumentMeta } from '../hooks/useDocumentMeta';
export function EcosystemPage(){useDocumentMeta('Our Companies & Brands | Divinityx Enterprises LLC','The governed portfolio of Divinityx Enterprises LLC.');return <><PageIntro eyebrow="Enterprise portfolio" title="Our Companies & Brands" copy="Every identity below is presented by its actual relationship to Divinityx Enterprises LLC—from registered operating divisions to brands, programs, media properties, and developing initiatives."/><div className="space-y-16">{portfolioGroups.map(group=><section key={group} aria-labelledby={`group-${group}`}><div className="section-heading"><p className="eyebrow">Portfolio classification</p><h2 id={`group-${group}`}>{group}</h2></div><div className="mt-7 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{getEntriesByGroup(group).map(e=><PortfolioCard key={e.id} entry={e}/>)}</div></section>)}</div></>}
