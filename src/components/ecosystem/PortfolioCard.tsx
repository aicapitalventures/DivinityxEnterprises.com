import { Link } from 'react-router-dom';import type { PortfolioRegistryEntry } from '../../types/portfolio';import { IdentityBadge } from './IdentityBadge';
export function PortfolioCard({entry}:{entry:PortfolioRegistryEntry}){const action=entry.operatingStatus==='in-development'?'Profile overview':'View profile';return <article className={`portfolio-card portfolio-card--${entry.accent}`}><IdentityBadge entry={entry}/><div className="flex flex-1 flex-col p-6"><div className="flex flex-wrap items-start justify-between gap-3"><p className="eyebrow">{entry.classification}</p><span className={`status ${entry.operatingStatus==='active'?'status--active':''}`}>{entry.operatingStatus==='active'?'Active':'In development'}</span></div><h3 className="mt-4 font-heading text-2xl leading-tight text-ivory">{entry.fullName}</h3><p className="mt-3 text-sm leading-relaxed text-ivory/68">{entry.description}</p><p className="mt-4 border-l border-crest/35 pl-3 text-xs leading-relaxed text-champagne/70">{entry.disclosure}</p><div className="mt-auto pt-6"><Link to={`/ecosystem/${entry.slug}`} className="text-link">{action} <span aria-hidden>→</span></Link></div></div></article>}
