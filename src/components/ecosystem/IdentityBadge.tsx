import type { PortfolioRegistryEntry } from '../../types/portfolio';
const tone={gold:'brand-stage--gold',blue:'brand-stage--blue',red:'brand-stage--red',white:'brand-stage--white'} as const;
export function IdentityBadge({entry,large=false}:{entry:PortfolioRegistryEntry;large?:boolean}){const master=entry.id==='divinityx-enterprises-llc';return <div className={`brand-stage ${tone[entry.accent]} ${large?'brand-stage--large':''}`}><div className="brand-stage__glow"/><img src={master?'/assets/brand/divinityx-enterprises-primary.png':entry.logoPath} alt={entry.logoAlt} width={master?941:undefined} height={master?1672:undefined} loading={master?'eager':'lazy'} fetchPriority={master?'high':'auto'} className={`relative z-10 max-w-full object-contain ${master?'max-h-96':'max-h-40'}`}/></div>}
