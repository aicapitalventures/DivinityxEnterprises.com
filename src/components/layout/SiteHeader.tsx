import { useEffect, useState } from 'react';
import { NavLink, useLocation } from 'react-router-dom';
const nav=[['/','Home'],['/ecosystem','Companies & Brands'],['/about','About'],['/contact','Contact']];
export function SiteHeader(){const [open,setOpen]=useState(false);const location=useLocation();useEffect(()=>setOpen(false),[location.pathname]);useEffect(()=>{const close=(event:KeyboardEvent)=>event.key==='Escape'&&setOpen(false);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);const link=({isActive}:{isActive:boolean})=>`block rounded-lg px-3 py-2 text-sm font-semibold transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-luminous ${isActive?'bg-heritage/35 text-champagne ring-1 ring-crest/25':'text-ivory/80 hover:bg-navy hover:text-luminous'}`;return <header className="sticky top-0 z-30 border-b border-crest/20 bg-cosmic/95 backdrop-blur-xl"><div className="relative mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-3 md:px-8"><NavLink to="/" aria-label="Divinityx Enterprises LLC home" className="group flex min-w-0 items-center gap-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-luminous"><img src="/assets/brand/divinityx-enterprises-primary.png" alt="" width="941" height="1672" loading="eager" fetchPriority="high" className="h-12 w-9 shrink-0 object-contain"/><span className="truncate font-heading text-sm tracking-[.08em] text-champagne sm:text-base">Divinityx Enterprises <span className="text-[.7em] text-luminous/75">LLC</span></span></NavLink><button type="button" className="rounded-lg border border-crest/60 px-3 py-2 text-sm text-champagne focus-visible:outline focus-visible:outline-2 focus-visible:outline-luminous md:hidden" aria-expanded={open} aria-controls="primary-navigation" onClick={()=>setOpen(value=>!value)}>Menu</button><nav aria-label="Primary" className="hidden items-center gap-1 md:flex">{nav.map(([to,label])=><NavLink key={to} to={to} className={link}>{label}</NavLink>)}</nav>{open?<nav id="primary-navigation" aria-label="Primary" className="absolute inset-x-4 top-full mt-2 border border-crest/30 bg-cosmic p-2 shadow-halo md:hidden">{nav.map(([to,label])=><NavLink key={to} to={to} className={link}>{label}</NavLink>)}</nav>:null}</div></header>}
